1 задание:
    1. запросить от пользователя название файла и создать в папке 'file/____.txt'
    2. запросить от пользователя 3 значения:
        - количество чисел
        - нач занчение
        - конечное значение
        -> вывести введенное количество рандомных числе в предлах нач и кон значения в вышесозданный файл
    3. должна быть проверка исключения во всех случаях
    4. в свой собственный модуль записать функции:
        - рандомных чисел
        - запись в файл реализовать как функцию
        - чтение файла как функцию
        - подсчет суммы
        - вызовы функции от пользователя
            * для чисел
            * для имени файла
    5. главный файл испольщовать через 'main'
    6. вывести сумму значений подсчитанных из файла

try:
    f = open()
finally:
    f.close()

with open() as f:
    #...